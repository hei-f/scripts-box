/**
 * WorkflowEditor 尺寸常量
 *
 * 统一管理工作流编辑器中使用的尺寸值，包括：
 * - 节点尺寸
 * - Handle 端口尺寸
 */

// ==================== 节点尺寸 ====================

/**
 * 脚本节点宽度
 */
export const NODE_WIDTH = 280;

/**
 * 输入节点宽度
 */
export const INPUT_NODE_WIDTH = 260;

// ==================== Handle 端口尺寸 ====================

/**
 * Handle 端口尺寸（宽度和高度相同）
 */
export const HANDLE_SIZE = 10;
